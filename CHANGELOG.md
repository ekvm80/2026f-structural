# 수정 이력

## 2026-09-10 — 개념 추가: M2-12 지점 조합과 반력

`이동 · 회전 · 고정` 세 지점(과 자유단)을 보의 양 끝에 자유롭게 조합하면서 반력을 직접 읽는 앱을 추가하였다. 기존 M2-08(지점의 종류)이 지점 하나를 밀고 돌려 보는 앱이라면, 이 앱은 조합 전체를 다루고 반력의 크기까지 계산한다.

- **조작** — A·B 지점 종류(없음/이동/회전/고정) 각각 4가지, 수직하중 P, 하중 위치 a, 수평하중 H. 스팬은 6 m로 고정.
- **반력 표시** — 구속된 방향에만 붉은 화살표(수평·수직)와 호 화살표(모멘트)를 그리고 크기를 함께 적는다. 구속되었으나 값이 0인 경우는 `H = 0`으로 따로 표시해 "구속 = 반력 발생"과 "이번 하중에서는 0"을 구분한다.
- **구속·허용 표시** — 각 지점 아래에 수평이동·수직이동·회전을 `●`(구속) / `○`(허용)로 표기.
- **불안정 판정** — 세 운동 중 하나라도 막히지 않으면 불안정으로 판정하고, 점선 고스트와 변위 화살표로 어느 방향으로 움직이는지 보여 준다. 회전 구속은 고정지점 하나 또는 서로 다른 위치의 수직 반력 두 개(짝힘)로 성립하는 것으로 판정한다.
- **반력 해** — 정정인 네 조합만 평형식으로 푼다. 회전+이동 `V_B = Pa/L`, 이동+회전 `V_A = P(L−a)/L`, 고정+자유단 `M_A = +Pa`(반시계), 자유단+고정 `M_B = −P(L−a)`(시계). 모멘트는 반시계 +. 수평하중은 보 축선에 작용시켜 모멘트에 기여하지 않는다.
- **부정정** — `r > 3`이면 (r−3)차 부정정으로 표시하고 반력은 계산하지 않는다.

### 대시보드·지도 반영
- `index.html`·단일파일: 필터 버튼의 개수 라벨을 하드코딩에서 **DATA에서 계산**하도록 바꾸었다. 앞으로 개념을 추가해도 숫자가 어긋나지 않는다.
- `map.html`: M2 항목 추가, MODULE 02 개수 11 → 12, 전체 개수 61 → 62.
- `README.md`: 개념 수와 분류 집계 갱신(필수 56 · 선택 6, 표준 29, 해석 20).
- M2-11 · M3-01의 이전/다음 링크를 M2-12로 이어 붙였다.

## 2026-09-08 — 물리·역학 정합성 전수 검토

61개 앱의 본문·수식·JS 모델·확인 문제를 모듈별로 검토하고, 물리·수학·단위·부호 규약이 잘못된 부분만 고쳤다(디자인·구조·요소 id는 그대로). 아래는 앱별 수정 항목이며, 수정하지 않은 참고 사항은 제외하였다.

## M1 구조와 역학의 큰 그림

### M1-01 하중의 흐름과 구조체 · 구조부재
- lab-h 텍스트: "기둥 폭 = 축력에 비례" → "기둥 단면적 ∝ 축력 (폭 ∝ √축력)" — JS는 `bw=5+15*sqrt(P/Pmax)`로 폭을 √P에 비례시켜 그리므로(응력 일정 가정에서 단면적 ∝ P) 기존 문구가 그림과 불일치.

### M1-03 역학의 3대 조건
- JS 그림: 하중 P 화살표가 판 아래에서 위로 향함(머리가 판 밑면, `plate+dd+42 → plate+dd+13`) → 판에서 아래로 향하도록 방향 반전. 봉이 인장(빨강)이고 판이 아래로 처지는 상태와 하중 방향이 모순이었음.
- JS 그림: P의 작용 위치를 항상 판 중앙(W/2)에 두던 것을 `xP = x1+(x2−x1)·F2/P`로 변경 — 판이 수평을 유지(δ₁=δ₂)하려면 ΣM=0에 의해 P의 작용점이 F₁:F₂ 비로 정해짐. k₁≠k₂일 때 중앙 하중은 판 회전을 유발하므로 그림이 적합조건 가정과 불일치했음.
- lab-h 텍스트에 "(E₂ = 200 GPa, L = 2 m 고정)" 추가 — JS에 고정된 E₂, L이 화면 어디에도 표시되지 않아 readout "공통 처짐 δ"와 강성비 값을 해석할 수 없었음.

### M1-04 강체와 변형체
- JS 처짐식: `δ = PL³/(3EI)`(고정단 캔틸레버식) → `δ = P·L²(a+L)/(3EI)` — 핀(A)·롤러(B) 위 내민보의 끝 처짐은 B점 회전(θ_B = PL·a/3EI)을 포함해야 함(Gere 부록 G, 내민보 끝 집중하중). 기본값(P=1.5 kN, L=2 m, a=1.2 m, E=10 GPa, 300×50 mm)에서 128 mm → 204.8 mm.
- JS 처짐 곡선: A~B 구간을 A에서 0, B에서 −0.16δ(위로)인 직선으로 그려 롤러 지점 B가 들려 올라가고 B에서 곡선이 불연속이었음 → 단부모멘트 PL을 받는 단순보의 위로 볼록한 곡선 `v(s) = −δ·s(a²−s²)/(2aL(a+L))`(A, B에서 0)로 교체. B~끝 구간도 B점 회전항 `a·t/(L(a+L))`을 포함하도록 수정(t=L에서 정확히 δ).
- JS 그림: A점 반력 화살표가 위로 향함(`y+52 → y+18`)이었으나 RA = P·L/a는 아래 방향(홀드다운)이고 옆 라벨도 '↓'였음 → 아래로 향하도록 수정.
- readout r1/r2: 크기만 표시 → "2.50 kN ↓", "4.00 kN ↑"로 방향 병기 — A 반력은 하향이므로 부호 없이는 오독.
- readout r3 및 본문: 강체 모델에서 "최대 모멘트 — 못 구함" → 항상 `P·L (B점)` 표시. 단면력(V, M)은 자유물체도의 강체 평형만으로 결정되며(Gere 4장, Hibbeler Statics 7장) 변형체 가정이 필요하지 않음. 이에 맞춰 목록 "정역학 (강체) — 지점 A·B의 반력. 여기까지가 전부입니다." → "지점 A·B의 반력, 그리고 자유물체도로 구하는 단면력(전단력·휨모멘트). 여기까지가 전부입니다.", readout 라벨 "최대 모멘트" → "최대 모멘트 (B점)", 캔버스 캡션 "정역학 — 반력까지" → "정역학 — 반력·단면력까지". Q&A의 "처짐 칸만 활성화됩니다"와도 일치하게 됨.

### M1-05 하중의 종류와 산정
- 설계에서 문단: "사무실(2.5)을 서고나 집회장(5.0)으로 바꾸면 활하중이 두 배가 되고" → "사무실(2.5)을 집회장(5.0)이나 서고(7.5)로 바꾸면 활하중이 두 배 이상이 되고" — KDS 41 10 15에서 서고(도서관 서가실) 활하중은 7.5 kN/m²(3배)이므로 '서고 = 두 배'는 부정확.

### M1-06 안전율과 세 가지 설계법
- JS `MAT.s`: 강재 기준강도 400 MPa(인장강도) / FS 1.5 → 허용응력 266.7 MPa로, SS400 항복강도(235~245 MPa)를 초과하는 물리적으로 불가능한 허용응력이 나왔음. Gere §1.8 및 KDS 허용응력설계법에 따라 연성 재료(강재)는 항복강도 기준: `s:[275,1.5,'강재 SS275 (항복강도 기준)']` → σ_allow = 183.3 MPa.
- 이에 맞춰 readout 라벨 "극한강도 σᵤ" → "기준강도 σᵤ (강재는 σy)", 캔버스 눈금 라벨 "극한 ○○ MPa" → "기준 ○○ MPa", `.eq`에 "(연성 재료인 강재는 σy / FS)" 추가.

### M1-07 단위 · 차원 · 접두어
- 오해 문단: "바닥하중은 kN/m²(≈kPa)" → "kN/m²(= kPa)" — 1 kN/m² = 1 kPa는 근사가 아닌 항등식.

## M2 힘 · 모멘트 · 평형

### M2-05 우력(짝힘)과 등가우력
- readout 라벨(텍스트): `왼쪽 힘의 모멘트` / `오른쪽 힘의 모멘트` → `위쪽 힘의 모멘트` / `아래쪽 힘의 모멘트` — 그림의 두 힘은 위·아래 두 수평 작용선 위에 있고, JS의 M1·M2는 각각 yTop·yBot 작용선의 힘에 대한 모멘트이므로 라벨이 그림·계산과 불일치했다.

### M2-06 합력과 등가 힘–우력계
- JS(그리기): 합력 R 화살표 `arrow(c,xr,y+110,xr,y+52,...)`(위쪽 방향) → `arrow(c,xr,y+52,xr,y+110,...)`(아래쪽 방향) — 네 하중이 모두 아래 방향인데 등가계의 합력이 위 방향으로 그려져 방향이 반대였다.
- JS(readout r4): R = 0일 때 `'R = 0 → 우력만'` → `'R = 0 (하중 없음)'` — 네 힘이 모두 같은 방향(≥ 0)이므로 R = 0이면 M_O도 0이 되어 우력이 남을 수 없다.

### M2-08 지점의 종류
- JS(그리기): 반력 화살표 조건 `if(Hx&&AC!=='n')`, `if(Vy&&AC!=='n')` → 각각 `&&!free` 추가 — 롤러를 수평으로 밀거나 핀·롤러를 회전시키는 '자유 운동' 경우 메시지는 "자유롭게 이동/회전합니다 → 반력 없음"인데 수직(및 수평) 반력 화살표가 함께 그려져 그림과 메시지가 모순되었다.

### M2-09 자유물체도
- JS(그리기, 예제 b 4단계): 자중 W 화살표 `arrow(c,mid,y+62,mid,y+12,...)`(위 방향) → `arrow(c,mid,y+12,mid,y+62,...)`(아래 방향) — 자중은 아래로 작용해야 하는데 위로 그려져 있었다. 예제 a의 하중 P는 원래대로 아래 방향이다.

### M2-11 정정 · 부정정 · 부적정 지지
- `.eq` small 텍스트: `r = n → 정정 · r > n → 부정정 · 반력이 평행하거나 공점이면 …` → `r = n → 정정 · r > n → 부정정 · r < n → 불안정(구속 부족) · 반력이 평행하거나 공점이면 …` — 앱의 (d) 롤러 2개, (e) 경사 롤러 2개는 r = 2 < 3(r − n = −1)인데 분류식에 r < n 경우가 없었고, 본문의 부적정 정의("개수가 맞아도 배치가 잘못된 경우")와도 맞지 않았다. Hibbeler는 r < n을 partially constrained(불안정)로, 개수가 충분해도 평행·공점인 경우를 improper constraints로 구분한다.

## M3 트러스와 골조

### M3-01 트러스의 정의와 세 가지 가정
- JS `buildTruss` 사재 생성(프랫/하우): 모든 칸에서 사재 방향이 동일(`[B+i, i+1]` / `[i, B+i+1]`) → 중앙을 기준으로 대칭(`i<bays/2` 이후 방향 반전) — 원래 코드는 우측 절반의 사재가 프랫에서 압축, 하우에서 인장이 되어(예: 4칸 프랫 사재 7-3 = −14.1 kN, 8-4 = −42.4 kN) 프랫(사재 인장)·하우(사재 압축)의 정의와 모순되고 좌우 비대칭 부재력(0-1 = 0, 5-6 = −30 kN)이 나왔다. 수정 후 4칸 프랫: 사재 42.4·14.1·14.1·42.4 kN 모두 인장, 수직재 모두 압축, 좌우 대칭.

### M3-02 격점법
- JS 풀이 순서 `seq=[0,5,1,6,2,7]` → `[5,0,1,6,7,2]` (F→A→B→G→H→C) — 원래 순서의 첫 절점 A(하현 좌단)는 부재 3개(0-1, 0-5, 0-6)가 만나 식 2개로 풀 수 없고, 5번째 절점 C도 그 시점에 미지수가 3개였다. 수정 순서에서는 6단계 모두 미지 부재력이 2개 이하임을 node로 확인(F: 2, A: 2, B: 2, G: 2, H: 2, C: 2).
- JS r2 "알려진 힘"·r3 "미지 부재력" 집계: 현재 절점을 `solved`에 포함시킨 채 `hide` 여부로 판정하여 항상 미지 0개·"가능"으로 표시되던 것 → 반대쪽 절점을 이미 풀었으면(`prev`) 알려진 힘, 아니면 미지로 집계.
- JS 압축 부재력 화살표: 절점에서 부재 반대편(절점 너머)으로 그려지던 것 → 부재 위에서 절점을 향하도록 위치 변경. 그림 하단 설명 "절점 쪽 = 압축"과 일치시킴. 영부재(|F| < 0.05 kN)에는 화살표를 그리지 않도록 함(원래는 0도 압축 화살표로 표시).
- JS `buildTruss` 프랫/하우 사재 대칭화 — M3-01과 동일 함수(이 앱은 워런만 사용하므로 표시 결과 변화 없음).

### M3-03 단면법
- JS 절단선 위치: 슬라이더 25 %, 50 %, 75 %에서 절단선이 절점을 정확히 통과해 사재 1개만 "잘린 부재"로 집계되어 "1개 부재 절단 — 풀 수 있습니다"가 표시됨 → 절점과 일치하면 0.5 px 비켜 놓아 항상 유효한 단면(3개 절단)이 되도록 함. 절점을 관통하는 선은 트러스를 두 조각으로 나누는 단면이 아니다.
- JS 사재 내력 화살표 위치: 계산해 두고 쓰지 않던 실제 교점 `yi` 대신 부재 중점 y를 사용해 화살표가 사재에서 벗어나 그려짐 → 교점 `yi` 사용.
- JS 압축 화살표: 절단면에서 남긴 쪽 안으로 그려지던 것 → 제거된 쪽에서 절단면을 향하도록(수식 설명 "압축력은 절단면을 향하고"와 일치). 라벨도 같은 쪽으로 이동.
- JS `buildTruss` 프랫/하우 사재 대칭화 — M3-01과 동일 함수(표시 결과 변화 없음).

### M3-04 영부재
- JS 규칙 ② 그림: 두 화살표가 절점을 향하도록 그려져 있어(압축 표현) 인장색과 모순 → 절점에서 바깥쪽으로(인장 가정) 반전.
- 텍스트 규칙 ① 및 r2: "하중 없는 절점" → "하중 없는 절점(지점 반력도 없는 절점)", r2 "없음 (필수 조건 · 반력 포함)" — Hibbeler의 규칙은 외부 하중과 지점 반력이 모두 없을 때 성립한다.

### M3-05 프레임과 기계의 해석
- JS 3단계 캔버스 문구 "부재 3개 × 식 3개 = 9개 → 접합부 미지수까지 풀 수 있습니다" → "식 9개 · 미지수 10개 (반력 4 + 강접합부 3×2) → 1차 부정정"; r4 "풀이 가능" → "1차 부정정 (식 9 < 미지수 10)"; 1단계 r4 "부정정 — 분해 필요" → "식 3 < 미지수 4" — 그림의 구조(기둥 하단 핀 2개, 강접합 모서리 V·N·M 쌍)는 2핀 라멘으로 1차 부정정이며, 강접합부를 절단하면 식 3개와 미지수 3개가 함께 늘어 정정이 되지 않는다. 같은 앱의 Q&A("새 미지수도 함께 생긴다", "내부 힌지가 있어야 정정")와도 모순이었다.
- 텍스트 목록 3항 "각 부재마다 평형식 3개가 새로 생겨 … 보충할 수 있습니다" → 접합부 내력이 미지수로 추가되며 핀 접합(2개)은 식이 1개 순증가, 강접합(3개)은 순증가 없음, 그림의 2핀 강접합 골조는 1차 부정정으로 남는다고 명시. 2단계 캔버스 문구도 동일 취지로 수정.

### M3-06 입체트러스
- JS 부재 색: 하중이 없는데 부재를 인장색·압축색으로 교대 채색 → 잉크색 단일 — 모듈 전체의 "붉은색 인장 · 파란색 압축" 규약과 충돌하여 무하중 상태에서 부재력 부호가 있는 것처럼 보였다.

## M4 응력과 변형률

### M4-01 수직응력
- JS(단면적 계산): 중공 원형 `Math.PI*(d*d-(d-2*t)*(d-2*t))/4` → `Math.PI*(d*d-Math.pow(Math.max(0,d-2*t),2))/4` — t ≥ d/2 이면 내경이 음수가 되어 (d−2t)² > d² → A < 0 → `A=1`로 대체되어 σ가 수만 MPa로 표시됨(예: d = 20 mm, t = 25 mm). 내경을 0으로 클램프하여 실체 원형으로 수렴.
- JS(단면 그림): 내경 원 반지름 `r*(d-2*t)/d` → `r*Math.max(0,d-2*t)/d` (2곳) — 위와 동일 사유.

### M4-02 수직변형률
- JS(r4 판정): `e1>0.002? '짧은 부재 항복':'둘 다 탄성'` → `e2>0.002? '둘 다 항복':(e1>0.002? '짧은 부재 항복':'둘 다 탄성')` — L₂ = 2 m, δ = 12 mm 이면 ε₂ = 6000 με > 2000 με로 긴 부재도 항복하는데 "짧은 부재 항복"만 표시되던 오류.

### M4-03 응력–변형률 선도
- JS(r4 상태): 콘크리트·목재에서 최고점 이후를 "넥킹 · 파단 임박"으로 표기 → 취성재료는 "최고점 이후 · 파괴 임박", 비례한도 초과 구간은 "비선형 (비례한도 초과)"로 분기 — 넥킹(단면 축소)은 연성재료 고유 현상이며 콘크리트·목재에는 발생하지 않는다.
- JS(r3 항복강도): 콘크리트·목재에서 `fy` 값(24, 36 MPa)을 "항복강도"로 표시 → "뚜렷한 항복 없음 (σ_u ○○ MPa)" — 콘크리트 24 MPa는 압축강도 f_ck이며 항복강도가 아니다.
- JS(그림 표시점 라벨): 취성재료에서 'σ_y 항복' → 'σ_pl 비례한도' — 위와 동일 사유(MAT.ey는 선형구간 종점 역할).

### M4-05 후크의 법칙과 탄성계수
- JS(그림 하중 화살표): `arrow(c,x,top+len+ex+52,x,top+len+ex+10,…)` → 시작·끝점 교환 — 부재는 아래로 늘어나게(ex 만큼 연장) 그려져 있고 색·헤더("늘어남")도 인장인데 화살표는 부재 끝을 향해(압축 방향으로) 그려져 있었다. 인장 방향(부재에서 멀어지는 방향)으로 수정. 'P' 글자 위치를 +64 → +70으로 이동해 화살촉과의 겹침 회피.

### M4-07 전단응력과 단전단 · 복전단
- JS(그림, 복전단 하중 화살표): 좌측 화살표가 y = cy − 0.5pl(두 외측판 사이 빈 공간)에 그려짐 → 복전단에서는 두 외측판(cy ∓ 1.3pl) 각각에 화살표를 그리도록 분기 — 복전단은 외측판 2장이 P를 분담하고 중앙판이 반대 방향 P를 받는 구조이므로 하중 작용 위치가 판과 일치해야 한다. 단전단 그림은 변경 없음.

### M4-08 전단변형률과 전단탄성계수
- JS(그림): `arrow(c,cx+bw/2+56,cy-bh,cx+bw/2+sx+6,cy-bh,ten,0,0)` 삭제 — 선폭 0·화살촉 0을 의도한 비활성 호출이지만 헬퍼가 `w||1.4`, `head||7`로 기본값을 적용해 상면 우측 끝에 좌향 화살표가 그려졌다. 상면 좌측의 우향 τ 화살표와 방향이 반대여서 전단 방향 표현이 모순되었다.
- JS(r2 각도 변화, 캔버스 텍스트): `g*180/Math.PI` → `Math.atan(g)*180/Math.PI` (θ, tan θ = γ) — 기본 상태(고무 G = 1 MPa, τ = 3 MPa)에서 γ = 3 rad을 "171.9°"로 표시하는데, 직각의 변화량은 90°를 넘을 수 없다. 단순전단 기하에서 γ = tan θ이므로 θ = atan γ가 정확하고, 강재·콘크리트(γ ~ 10⁻⁵)에서는 결과가 동일하다. 텍스트를 "γ = … rad, θ = …°"로 구분 표기.

### M4-09 지압응력
- JS(파괴 모드 판정, 3곳): `ed<1.5` → `ed<2` — 본문이 "연단거리 규정(보통 e ≥ 2d)"과 "e를 줄여 보세요 — 파괴 모드 예측이 바뀝니다"라 설명하는데 JS는 e/d < 1.5에서만 연단파괴를 예측해 e/d = 1.5 ~ 2.0 구간에서 본문과 판정이 모순되었다. AISC 기준으로도 연단 찢김(1.2 L_c t F_u)이 지압(2.4 d t F_u)보다 먼저 지배하는 경계는 e ≈ 2.5d이므로 2d가 물리적으로도 더 가깝다.
- 텍스트(r3 라벨): "지압 판정" → "지압 판정 (허용 480 MPa)" — JS 판정 기준값 480 MPa(≈ 1.2 f_u, ASD 지압 허용응력)이 화면 어디에도 표시되지 않아 OK/NG 근거를 알 수 없었다. M4-07의 "판정 (허용 150 MPa)" 표기와 형식 통일.

### M4-10 허용응력 · 허용하중과 단면 설계
- JS(재료표): 강재 `[410, 1.5]` → `[275, 5/3]` — 기존 σ_allow = 410/1.5 = 273 MPa는 항복강도 275 MPa와 거의 같아 안전율이 사실상 1.0인 허용응력이며 공학적으로 성립하지 않는다. Q&A는 165 MPa(= 0.6 f_y, 고전적 ASD)로 풀고 "위에서 P = 200, 강재, 치수 35로 확인해 보세요"라 안내하는데, 앱은 A_req = 732 mm²(한 변 27 mm)를 표시해 Q&A(1212 mm², 34.8 mm)와 불일치했다. 수정 후 앱 값 165 MPa, 1212 mm²로 Q&A와 일치.
- 수식(eq): "σ_allow = σ_u / FS" → "σ_allow = σ_u / FS (강재는 σ_y / FS)" — 강재의 기준강도를 항복강도로 바꾼 데 따른 정합.
- 텍스트(설계 절차 2·3단계): "극한강도 σ_u와 안전율 FS" → "기준강도(σ_u 또는 σ_y)와 안전율 FS"; 3단계에 "(강재는 σ_y / FS)" 추가 — 동일 사유.
- lede 변경(대시보드 데이터 중복 주의): "극한강도를 안전율로 나눈 허용응력으로" → "재료 강도(극한강도 또는 항복강도)를 안전율로 나눈 허용응력으로" — 동일 사유. `<h1>`, `<span class="en">`은 변경하지 않았다.

## M5 축하중 부재

### M5-01 축부재의 신장량과 강성
- 텍스트(설계에서): "네 변수 중 L만 δ를 키우고 나머지 셋은 줄입니다." → "네 변수 중 P와 L은 δ를 키우고 E와 A는 줄입니다." — δ = PL/(EA)에서 P도 δ를 키운다. 캔버스의 기여도 막대(P, L = 붉은색)와도 모순되었다.

### M5-02 불균일 조건에서의 변형
- JS(자중 단위): `77e-9*a*Ls` → `77e-6*a*Ls` — 77 kN/m³ = 77 × 10³ N / 10⁹ mm³ = 77 × 10⁻⁶ N/mm³. 기존 계수는 자중을 1000배 작게 계산하였다(A = 1000 mm², 2 m 구간의 자중 154 N이 0.154 N으로 계산됨).
- JS(그림): 지지선·해치를 기둥 상단(천장)에서 하단(지반)으로 이동 — 모델은 상단 하중 P가 아래로 누적되는 압축 기둥(N이 하단에서 최대)인데, 그림은 천장에 매달린 봉으로 그려져 축력도·색상(압축)과 지지 조건이 불일치하였다.
- 텍스트(readout 라벨): "총 신장량 Σδ" → "총 축소량 Σδ" — 압축 기둥의 변형은 축소이며, 부호 규약(신장 +)과 '설계에서' 문단의 "축소량(column shortening)"과 일치시킴.

### M5-03 부정정 축부재의 해석
- JS(그림): C점 하중 P를 수평 화살표(횡하중처럼 보임) → 수직 하향 화살표로 변경 — 축하중 문제이며, 모델(상부 인장·하부 압축, R_A + R_B = P)은 하향 P를 전제한다.
- JS(그림): R_B 화살표를 하향 → 상향으로 변경(라벨 위치도 지지선 아래로 이동) — 봉의 자유물체도에서 두 반력은 모두 위를 향해야 R_A + R_B = P가 성립한다. 기존 그림은 R_A 상향, R_B 하향으로 평형식과 모순되었다.
- 수식 small 텍스트: "양단 고정 봉의 중간에 하중이 걸린 경우 — 반력은 반대쪽 거리에 비례해 갈립니다." → "양단 고정 균일 단면 봉의 중간에 하중이 걸린 경우 — 반력은 반대쪽 구간의 길이(일반적으로는 유연도 L/EA)에 비례해 갈립니다." — R_A = Pb/L은 균일 단면에서만 성립하며, 앱은 단면비 A₂/A₁ 슬라이더를 제공하므로 A₂/A₁ ≠ 1이면 readout이 표시된 식과 달라진다.

### M5-04 온도변화와 열응력
- 텍스트(설계에서): "길이를 60 m로 놓고 구속 조건을 '양단 고정'으로 바꿔 보세요 — 하중 없이도 응력이 항복강도에 육박합니다." → "ΔT를 60 ℃로 놓고 구속 조건을 '양단 구속'으로 바꿔 보세요 — 하중 없이도 항복강도의 절반을 넘는 응력(144 MPa)이 생깁니다." — 완전 구속 열응력 σ = EαΔT는 L과 무관하다(같은 파일의 '자주 하는 오해'가 이를 명시). 또 슬라이더 범위 내 최대값은 200000 × 12 × 10⁻⁶ × 60 = 144 MPa로 항복강도 275 MPa의 52 %이며 "육박"이 아니다.
- JS(판정): 콘크리트 판정 한계를 압축(24 MPa)·인장(3 MPa)으로 분리하고, 판정 문구를 강재 "항복 초과!", 콘크리트 "압축강도 초과!"/"인장강도 초과(균열)!"로 구분 — 기존 코드는 콘크리트 인장(냉각 시)에도 24 MPa를 적용하여 ΔT = −40 ℃(σ = +10.8 MPa 인장)에서 "탄성 범위"로 표시하였다. 콘크리트 인장강도는 약 0.63√f_ck ≈ 3 MPa 수준이며, 콘크리트에는 "항복"이라는 표현도 부적절하다.

### M5-05 경사단면의 응력
- JS(그림): 절단선 방향을 (좌하→우상)에서 (좌상→우하)로 변경 — 코드의 법선 n = (cosθ, −sinθ)(화면 좌표, θ는 x축에서 반시계)와 기존 절단선 방향 (sinθ, −cosθ)의 내적이 2 sinθ cosθ ≠ 0으로, σ_θ 화살표가 절단면에 수직이 아니었다(θ = 45°에서 화살표가 절단선과 평행). 수정 후 내적 0을 θ = 0°, 30°, 45°, 60°, 89°에서 확인하였다.
- JS(그림): τ_θ 화살표 방향 반전 — 좌측 자유물체의 경사면에 작용하는 트랙션은 σ_x cosθ·(1, 0)이며, 이를 법선 성분 σ_x cos²θ(+n 방향)와 접선 성분 −σ_x sinθ cosθ(−t 방향)로 분해하면 τ는 우하향이어야 한다. 기존에는 σ_θ는 좌측 물체의 면, τ_θ는 우측 물체의 면 기준으로 그려져 두 화살표의 합이 수평이 되지 않았다. 라벨 위치도 함께 이동하였다.
- JS(경계값): θ → 90°에서 tanθ 발산으로 절단선 좌표가 10¹⁶ px에 이르던 문제를 절단선 반길이를 (x₁−x₀)/2로 제한하고 기울기를 유지하도록 수정.
- 텍스트(자주 하는 오해): "축에 수직인 면(θ=0)에서만 전단이 0일 뿐, 기울어진 면에는 언제나 전단이 있습니다." → "축에 수직인 면(θ = 0°)과 축에 평행한 면(θ = 90°)에서만 전단이 0일 뿐, 그 사이의 기울어진 면에는 언제나 전단이 있습니다." — τ_θ = −σ_x sinθ cosθ는 θ = 90°에서도 0이다.

## M6 보의 내력

### 공통 보 해석기 `beam()` (M6-01 ~ M6-04에 동일하게 포함, 4개 파일 동일 수정)
- JS `beam()` M(x) 모멘트 하중 항: `if(x>l.x) m-=l.M;` → `if(x>l.x) m+=l.M;` — 반력 계산은 `Rb=(mom+mo)/xb`로 모멘트 하중을 시계방향 +로 취하는데, M(x)에서는 반대 부호로 점프시켜 단순보에서 M(L) = −2M₀, 캔틸레버에서 하중 우측 M = −2M₀가 나오는 모순이 있었다(정답은 각각 0). 수정 후 M(L)=0, 좌측 −M₀x/L, 우측 M₀(1−x/L) 확인. 현재 5개 앱 중 모멘트 하중을 실제로 쓰는 앱은 없어 화면상 영향은 없으나 잠재 오류이므로 수정. 주석에 「(M: 시계방향 +)」 명시.
- JS `beam()` V(x) 집중하중 항: `if(x>l.x) v-=l.P;` → `if(x>l.x || l.x<=0) v-=l.P;` — 집중하중이 지점 A 위(x = 0)에 놓이면 하중이 지점으로 직접 전달되어 내부 전단력은 0이어야 하는데, 격자점 x = 0에서 V(0) = R_A(= P 포함)가 되어 |V|max 판독값이 P만큼 과대 표시되고 SFD 왼쪽 끝에 가짜 스파이크가 생겼다(M6-04의 P₁·P₂ 위치 슬라이더 최소값 0에서 재현). 수정 후 P@0 케이스에서 vmax = 0, 캔틸레버 M_A = 0 확인.

### M6-01 보의 종류 · 하중 · 반력
- JS 판독 r2(캔틸레버일 때): `'M_A '+fmt(|M_A|,1)` → `'M_A '+fmt(|M_A|,1)+' kN·m'` — r1은 kN 단위를 붙이는데 고정단 모멘트만 단위가 없었다. M6-04와 동일 형식으로 통일.

### M6-03 하중 · 전단력 · 휨모멘트의 관계
- JS V=0 위치 탐색: 부호가 바뀌는 격자 구간의 중점 `x0v=(xa+xb2)/2`를 그대로 표시 → 구간 안에서 40회 이분법으로 정밀화 — 기본값(w = 10 kN/m, L = 10 m, P = 0)에서 V = 0인 위치가 정확히 5.00 m인데 격자(0.025 m) 중점 때문에 「4.99 m」로 표시되었고, 집중하중 점프의 경우 「7.01 m」처럼 어긋났다. 수정 후 5.000 / 7.000 / 6.200 m(w + P 조합, 해석해 6.2 m) 확인.

### M6-05 골조의 축력 · 전단력 · 모멘트도
- JS 수직하중 절점 모멘트: `Mj_v = wL²/12 / (1 + 2h/L)` → `wL²/12 / (1 + h/(2L))` — 절점 평형 4EIθ/h + 2EIθ/L = wL²/12 에서 나오는 정해. 기본값에서 32.0 → 51.2 kN·m(강성해석 51.2). 극한 검증: h→0 에서 wL²/12(양단고정보), h→∞ 에서 0(단순보).
- JS 수직하중 기둥 밑면 모멘트: `Mcol_v = Mj_v` → `Mb_v = Mj_v/2` — 상단 회전만 있는 고정단 기둥의 원단 모멘트는 근단의 1/2(복곡률). 기본값 32.0 → 25.6 kN·m.
- JS 수평하중 기둥 모멘트: 포털법 `Mj_h = Mb_h = Ph/4`(h/L에 무관) → 처짐각법 정해 `Mt_h = 3Ph²/(2(L+6h))`, `Mb_h = Ph/2 − Mt_h` — 수직하중은 강성비를 반영한 해로 표시하면서 수평하중만 근사법을 쓰는 것은 비일관적이고, 층고 슬라이더로 h/L을 바꾸는 앱의 취지(강성비 효과)를 포털법은 표현하지 못한다. 기본값에서 상단 20.0 → 15.0, 밑면 20.0 → 25.0 kN·m(강성해석 일치). 극한 검증: L→0(보 무한강성)에서 Ph/4로 포털법에 수렴, L→∞에서 밑면 Ph/2(캔틸레버 2개).
- JS 기둥 축력 좌·우 뒤바뀜: `Nl = Nv + Nh, Nr = Nv − Nh` → `NL = Nv − Nh, NR = Nv + Nh` — P가 좌측 절점을 오른쪽으로 미는 그림이므로 전도모멘트는 풍상측(좌) 기둥의 압축을 줄이고 풍하측(우)을 늘린다. 축력 증감 Nh는 2·Mt_h/L(보 전단)로 계산. 기본값 좌/우 53.0/43.0 → 44.25/51.75 kN(강성해석 일치).
- JS 모멘트도 작도(보): 양단에 같은 부호·같은 크기 `−Mj`를 두고 대칭 포물선을 그림 → 좌단 −Mj_v + Mt_h, 우단 −Mj_v − Mt_h 의 직선보간 + wx(L−x)/2 — 수평하중 성분은 역대칭이므로 좌·우 절점 모멘트가 다르다(36.2 / 66.2 kN·m). 또 `moveTo(gxl, gyt−Mj·sc)` 뒤 첫 점이 `gyt+Mj·sc` 여서 좌측 절점에 길이 2Mj의 가짜 수직선이 그려지던 것을 제거.
- JS 모멘트도 작도(기둥): 좌측 기둥은 상단 바깥·밑면 안쪽, 우측 기둥은 상단·밑면 모두 바깥쪽에 그려져 (i) 좌·우가 비대칭이고 (ii) 보는 압축측, 기둥은 인장측에 그려 강절점에서 모멘트도가 이어지지 않았다 → 전체를 압축측으로 통일(보: + 위쪽, 기둥: 바깥면 인장이면 안쪽). 절점에서 보 단부와 기둥 상단 종거가 같은 쪽·같은 크기로 이어지며, 두 기둥 모두 복곡률(상단·밑면 부호 반대)로 표시된다. 폭 0 선(`line(..., comp, 0)`) 잔재 삭제.
- JS 전단력도: 보 전단에 수평하중에 의한 상수항 −2Mt_h/L 누락, 기둥 전단을 좌·우 동일 `P/2 + 2Mj_v/h`로 표시 → 보 V(x) = wL/2 − wx − 2Mt_h/L, 기둥 Q_L = P/2 − H_v, Q_R = P/2 + H_v(H_v = (Mj_v + Mb_v)/h = 1.5Mj_v/h; 수직하중 시 밑면 수평반력은 안쪽 방향). 기본값 Q_L = −9.2, Q_R = 29.2 kN(강성해석 일치). 다이어그램에 부재선까지 닫는 선 추가.
- JS 축력도: 보 축력(압축 P/2 + H_v = 29.2 kN)이 그려지지 않았고 기둥 색이 값과 무관하게 고정 → 보 축력 추가, 기둥은 압축이면 압축색·인장이면 인장색.
- 판독 r1·r2·r4: 절점·밑면 모멘트를 단일값(우측 절점값에 해당)으로 표시 → 좌/우 두 값 표시. `.rk` 라벨을 「기둥 축력 (좌 / 우)」「절점 모멘트 (좌 / 우)」「밑면 모멘트 (좌 / 우)」로 변경. 기본값: 축력 44.3 / 51.8 kN, 절점 36.2 / 66.2 kN·m, 보 중앙 44.8 kN·m, 밑면 0.6 / 50.6 kN·m.
- 텍스트(본문 목록): 「수평 하중이 걸리면 보가 기둥 사이를 잇는 타이 역할을 합니다.」 → 「…수평력을 전달하는 축력 부재(스트럿 또는 타이) 역할을 합니다. 위 예에서는 보가 압축을 받습니다.」 — 좌측 절점을 미는 P에 대해 보는 P/2 + H_v의 압축을 받으므로 「타이(인장재)」는 부정확.
- 텍스트(본문 목록): 「모멘트도가 역대칭이 되어 절점 위아래로 부호가 뒤집힙니다.」 → 「…기둥은 상단과 밑면에서 모멘트 부호가 뒤집히는 복곡률로 휩니다.」 — 강절점(부재 2개, 절점 모멘트 없음)에서는 보 단부와 기둥 상단 모멘트가 같아 부호가 뒤집히지 않는다. 뒤집히는 것은 기둥의 상단↔밑면.

## M7 보의 응력

### M7-03 도심 · 단면2차모멘트 · 단면계수
- 텍스트(lab-h 부제): "같은 면적, 다른 I" → "면적당 효율 I/A로 비교" — 비교 대상 4개 단면(직사각형·눕힘·I형강·강관)의 면적이 같지 않다(b = 200, h = 400, t = 12일 때 A = 80,000 / 80,000 / 12,069 / 14,627 mm²). 실제로 비교하는 양은 면적당 효율 I/A이므로 사실에 맞게 고쳤다.

### M7-04 휨응력 공식
- 텍스트(설계에서): "같은 M, 비슷한 면적인데 σ_max가 확 떨어집니다" → "같은 M에서 면적은 직사각형의 약 1/7인데 σ_max는 약 3배에 그칩니다. 단위 면적당 단면계수 S/A가 약 2배 큰 것으로, ..." — 앱 기본값(M = 80 kN·m, b = 200, h = 400, t = 12)에서 직사각형은 A = 80,000 mm², σ_max = 15.0 MPa, I형강은 A = 12,069 mm², σ_max = 48.8 MPa이다. 면적이 비슷하지도 않고 σ_max가 떨어지지도 않으므로(오히려 3.3배 증가) 원문은 앱 동작과 모순이다. S/A: 66.7 → 135.7 mm(2.04배).

### M7-05 휨에 대한 보의 설계
- JS: 직사각형(RC·목재) 허용응력 `sa = 60` → `sa = 10` MPa, 그림 라벨 "허용응력 60 MPa (RC 환산)" → "허용응력 10 MPa (목재 · RC 개략 환산)" — 60 MPa는 목재 허용휨응력(약 8 ~ 12 MPa)이나 RC 총단면 등가응력(M/(bh²) ≈ 1.5 ~ 4 MPa → 6M/(bh²) ≈ 10 ~ 25 MPa) 어느 쪽에도 해당하지 않는 비현실적 값이다. 또 앱 내부 서술과도 모순되었다: (i) Q&A의 "RC라면 허용응력이 훨씬 낮아 춤이 3~4배 필요"는 √(165/60) = 1.66배에 불과했으나 10 MPa에서는 √(165/10) = 4.06배로 일치한다. (ii) "경험적 출발점 RC h ≈ L/10 ~ L/12 … 위 계산기로 검증해 보세요"가 60 MPa에서는 L = 8 m, w = 20 kN/m, b = 200 mm에 h_req = 283 mm(L/28)가 되어 성립하지 않았고, 10 MPa에서는 h_req = 693 mm(L/11.5)로 성립한다.

### M7-06 보의 전단응력
- JS(r4 "웨브 분담률"): 고정값 `SK==='ibeam'? 0.93 : 0.67` → I형강은 (1/I)∫_web Q dy 로 수치 계산, 직사각형은 "– (플랜지 없음)" 표시 — 웨브 분담률은 단면 형상에 따라 변하는 양인데 상수로 표시되었고, 직사각형에는 웨브/플랜지 구분이 없어 67 %라는 값 자체가 의미가 없다. 검증: bf = 200, h = 400 → 96.9 %; bf = 400, h = 100 → 83.2 %; bf = 60, h = 800 → 99.2 %. 직사각형에 같은 적분을 전 단면에 적용하면 100.0 %가 되어 식의 정합성을 확인하였다.

## M8 응력 · 변형률 해석

### M8-01 평면응력과 응력요소
- JS `elem()` → `nrm()` (수직응력 화살표): 인장(σ ≥ 0)일 때 화살표 머리가 요소 면을 향해 그려지고(압축 모양), 압축일 때는 화살표가 요소 내부에 그려짐 → 두 경우 모두 요소 바깥에 배치하고, 인장은 면에서 바깥으로(머리 바깥), 압축은 바깥에서 면으로(머리 면 쪽) 향하도록 변경 — Gere 부호규약(인장 양, 화살표는 면에서 멀어지는 방향).
- JS `elem()` → `shr()` (전단응력 화살표): 캔버스 y축 뒤집기 부호 오류로 ±x면의 τ 방향이 반대로 그려짐(τxy > 0에서 +x면 아래, −x면 위, +y면 오른쪽, −y면 왼쪽 → 네 힘의 모멘트가 모두 같은 방향, 합모멘트 ≠ 0) → `mid[1]+d*L*ty*0.5 … mid[1]-d*L*ty*0.5`의 부호를 교환. 수정 후 τxy > 0에서 +x면 위, −x면 아래, +y면 오른쪽, −y면 왼쪽(모멘트 합 0, Gere Fig. 7-1과 일치). 회전 요소(θ ≠ 0)에서도 x1·y1 국부축 기준으로 일관됨을 확인.

### M8-02 응력의 변환식
- JS `elem()` `nrm()`/`shr()`: M8-01과 동일한 수정(공통 블록이 각 파일에 복제되어 있음).
- 수식 박스 `<small>` 텍스트: "180°가 아니라 90°마다 같은 패턴이 반복" → "360°가 아니라 180°마다 같은 패턴이 반복" — σθ, τθ는 2θ의 함수이므로 θ에 대한 주기는 180°. 본문 목록("주기가 180°")과도 모순이었음.

### M8-03 주응력과 최대전단응력
- JS `elem()` `nrm()`/`shr()`: M8-01과 동일한 수정.
- JS 균열선 오프셋: `ox=cx - off*Math.sin(ang)*-1, oy=cy - off*Math.cos(ang)` → `ox=cx + off*Math.sin(ang), oy=cy + off*Math.cos(ang)` — 기존 오프셋 방향이 균열선(주인장에 수직)의 법선이 아니어서, 순수전단(θp = 45°)처럼 cos 2·ang = 0인 경우 다섯 균열선이 한 줄로 겹쳐 그려짐. 수정 후 오프셋이 σ1 방향(cos θp, sin θp)과 일치(내적 0 확인).

### M8-04 모어원
- JS `elem()` `nrm()`/`shr()`: M8-01과 동일한 수정.

### M8-05 평면변형률과 3축응력
- 텍스트 `<h2>` 목록: "3축응력 — 세 방향 모두 구속" → "3축응력 — 세 방향 모두 응력이 작용" — 세 방향 모두 변형이 구속되면 ε = 0(변형 없음)이 되어 앱 모델(σz = (σx+σy)/2 부여)과도, 3축응력의 정의와도 맞지 않음.
- 텍스트 "설계에서": "“3축 구속”을 골라 보세요 — 변형률이 급격히 줄어듭니다. 이것이 횡구속 콘크리트가 강해지는 정량적 이유입니다" → "“3축 구속”(σ_z = (σₓ+σ_y)/2로 가정)을 골라 보세요 — σ_z가 더해지면 포아송 효과로 ε_x, ε_y가 평면응력일 때보다 작아집니다. 횡구속 콘크리트가 강해지는 원리도 이 3축 응력 상태와 관련됩니다" — 기본값(100, 50, ν = 0.3, E = 200 GPa)에서 ε_x 425 → 312.5 με, ε_y 100 → −12.5 με, ε_z −225 → +150 με로 "급격히 줄어듦"이 성립하지 않고, 횡구속 강도 증가는 후크 법칙의 변형률 감소가 아닌 3축 압축 하의 파괴조건에서 나오므로 인과 서술이 부정확.
- JS 캔버스 캡션: `'3축 구속 — 변형 크게 억제'` → `'3축응력 — σ_z = (σₓ+σ_y)/2 가정'` — 위와 같은 근거.

## M9 처짐

### M9-01 처짐곡선과 사용성
- JS(EI 단위 변환, 497~498행): `var EI=E*I/1e12; EI=E*I/1e9/1000;` → `var EI=E*I/1e9;` — N·mm² → kN·m² 변환은 N→kN(/1e3)과 mm²→m²(/1e6)으로 /1e9이다. 기존 코드는 /1e12로 EI가 1000배 작아져 기본값(L = 8 m, w = 20 kN/m, 250×500 mm, E = 27 GPa)에서 δ = 15,170 mm로 표시되었다(정답 15.17 mm, EI = 70,313 kN·m²). 캔버스 하단의 EI 표기 값도 함께 바로잡힌다.
- JS(단순보 처짐곡선 정규화, 513행): `t(1−2t²+t³)/(5/24)` → `/(5/16)` — 단순보 등분포 처짐형상 t(1−2t²+t³)의 중앙값(t = 0.5)은 0.3125 = 5/16이다. 기존 나눗수 5/24로는 중앙값이 1.5가 되어 `Math.min(1.2, sh)`에 걸려 곡선이 납작하게 잘리고 δ 화살표 길이(amp)와 곡선 최저점이 일치하지 않았다. 캔틸레버 형상 t²(6−4t+t²)/3은 올바르다.

### M9-02 모멘트–곡률 관계
- 텍스트(핵심 수식 `<small>`): "위로 볼록이 +, 반시계 처짐각이 +" → "v는 위 방향 +, 아래로 볼록(v″ > 0, 정모멘트)이 +, 반시계 처짐각이 +" — EI v″ = M에서 정모멘트(상연 압축, 처짐 위 방향 +)는 v″ > 0, 즉 아래로 볼록(∪) 형상이다. "위로 볼록(∩)"은 v″ < 0으로 부호가 반대이다.
- JS(처짐 도표, 514행): `function(x){return -VV(x)*1000;}` → `VV(x)*1000` — `diagram()`은 위쪽을 +로 그리므로, 부호를 뒤집으면 하향 처짐이 기준선 위쪽에 그려져 θ 도표(좌단 음, 우단 양: v′ 부호와 일치)와 v 도표가 서로 모순되었다. 수정 후 처짐이 아래쪽으로 그려져 하중 방향·θ = v′ 관계와 일치한다.

### M9-03 이중적분법
- JS(캔틸레버 처짐곡선, 442행): `sh = t²(3−t)/2` → `sh = (1−t)²(2+t)/2` — 캔버스에서 자유단이 좌측(t = 0), 고정단이 우측(t = 1)인데 기존 식은 고정단이 t = 0인 형상이어서 처짐이 고정단에서 최대·자유단에서 0으로 좌우가 뒤집혀 그려졩고, 6단계에서 δ 화살표(자유단 x0)와 곡선이 맞지 않았다. 수정 식은 t = 0에서 1, t = 1에서 0이며 t = 1에서 기울기 0(고정단 수평)을 만족한다.
- JS(단순보 처짐곡선 정규화, 443행): `/(5/24)` → `/(5/16)` — M9-01과 동일한 정규화 오류(중앙값 1.5 → 1.2로 잘림).

### M9-04 모멘트면적법
- JS(도심 위치, 446행): `xc = x0+(x1−x0)*(1−xbar/L)` → `xc = x0+(x1−x0)*(xbar/L)` — `xbar`는 자유단(x0) 기준 도심거리(집중하중 2L/3, 등분포 3L/4)인데 (1−xbar/L)을 쓰면 삼각형 M/EI 도의 도심이 자유단에서 L/3 지점(높이 0인 쪽)에, 포물선은 L/4 지점에 표시되어 그림·x̄ 치수선이 계산값과 어긋났다. 수정 후 도심점과 x̄ 치수선이 계산값(3.33 m, 3.75 m)과 일치한다.

### M9-05 공액보법
- JS(캔틸레버 공액보 하중 형상, 443·446행): `v = 1−t`, `v2 = (1−t2)` → `v = t`, `v2 = t2` — 실제보는 좌측 자유단(x0)에 P, 우측(x1) 고정이므로 M/EI는 x0에서 0, x1(고정단)에서 최대 PL/EI인 삼각형이다. 기존 코드는 반대로 자유단 쪽이 최대로 그려져 실제보의 BMD와 모순되었다. 계산값(θ = PL²/2EI, δ = PL³/3EI)은 형상과 무관하게 이미 정확했다.
- Q&A(단순보 중앙 집중하중): "총 면적 = PL²/16EI가 가상 하중이고 … 반력은 각각 절반 = PL²/16EI" → "총 면적 = PL²/8EI … 각각 절반 = PL²/16EI" — 삼각형 면적 ½·L·(PL/4EI) = PL²/8EI이다. 총 면적과 그 절반이 같은 값으로 적혀 있던 오류. JS(`area = Mmax·L/2/EI`, `th = area/2`)는 이미 정확하였다.

## 기타

### map.html (개념 지도)
- M3-06 정의문: "조인트법·단면법을 3차원 평형식(6개)으로 확장" → "조인트법은 절점당 3개, 단면법은 분할부당 6개의 3차원 평형식으로 확장" — 절점법의 3차원 평형식은 절점당 3개(공점력계)이다.

### M8-05 세그먼트 라벨
- "3축 구속" → "3축응력" — 모델은 σ_z = (σₓ+σ_y)/2를 부여할 뿐 변형을 구속하지 않으므로 라벨을 모델과 일치시켰다.

### M1-04 처짐 그림 (2차 수정)
- 처짐 곡선을 점마다 `Math.min(dy, 0.28H)`로 잘라 큰 하중·긴 내민 길이에서 보 끝이 수평선으로 왜곡되던 것 → 곡선 전체에 하나의 표시 배율 k = min(1, 0.28H/δ)를 적용해 형상을 유지하고, 축소 시 "처짐 그림 배율 1/n"을 표기.
- 하중 P 화살표·라벨이 변형 전 위치에 고정되어 있던 것 → 처진 끝점(yTip)을 따라 이동.

### M6-05 레이아웃
- 내력도 패널이 골조의 우측 기둥과 겹치고 P 라벨이 캔버스 왼쪽으로 잘리던 것을 좌표만 조정(골조 중심 0.34W→0.36W, 내력도 중심 0.66W→0.74W·폭 0.28W→0.24W, P 라벨 중앙 정렬)하여 겹치지 않게 하였다. 물리 오류는 아니나 수정된 내력도를 읽을 수 있어야 하므로 함께 고쳤다.

### 파생 파일
- `구조역학_개념실험실_단일파일.html`과 `index.html`의 DATA(M4-10 lede)를 앱 파일로부터 재생성하였다.
